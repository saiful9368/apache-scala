scala> :paste
// Entering paste mode (ctrl-D to finish)

import scala.io.StdIn.readLine

def concat(a: String, b: String): String = {
  val z = a + " " + b
  z
}

val a = readLine("Enter first string: ")
val b = readLine("Enter second string: ")

println(concat(a, b))
println(concat(a, b).length())


// Exiting paste mode, now interpreting.

saiful  Rahhmanring: Rahhman
15
import scala.io.StdIn.readLine
def concat(a: String, b: String): String
val a: String = "saiful "
val b: String = Rahhman
